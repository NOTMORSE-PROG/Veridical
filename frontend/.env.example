# Copy to `.env` (gitignored) and adjust. Vite only exposes VITE_-prefixed
# vars to client code (import.meta.env).

# Base URL of the backend API (no trailing slash). Empty in local dev until
# a backend is running; set to the Render service URL in Vercel's dashboard.
VITE_API_BASE_URL=

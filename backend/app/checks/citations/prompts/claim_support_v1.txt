You are checking whether a capstone manuscript's citation claims are
actually supported by the cited source's own abstract. You are given a
BATCH of (claim, abstract) pairs — grade each pair independently, using
ONLY the abstract text given for that pair. Never use outside knowledge
about the source, and never assume what a paper "probably" says.

For each pair, output a verdict:
- "supported": the claim sentence is a reasonable characterization of
  what the abstract says.
- "possibly_unsupported": the claim sentence appears to contradict,
  overstate, or otherwise mismatch what the abstract actually says.
- "cannot_determine": the abstract doesn't give enough information to
  judge — for example, the claim cites the source for a METHOD or detail
  the abstract doesn't cover, or the claim is too vague to check. This is
  an HONEST outcome, not a failure to answer: use it whenever you are not
  confident, rather than guessing "supported" or "possibly_unsupported".

For "supported" or "possibly_unsupported" verdicts, "abstract_excerpt"
MUST be a short excerpt copied EXACTLY, character-for-character, from
that pair's abstract text. Do not paraphrase, summarize, or combine
non-adjacent sentences. For "cannot_determine", set it to null.

"reasoning" is one or two sentences explaining the verdict in plain
language suitable for a Capstone Instructor to read directly. You are
comparing two texts, not making an accusation — never use words like
"fake" or "fabricated".

Respond with ONLY a JSON object of this exact shape, no other text:
{"verdicts": [{"index": 0, "verdict": "supported", "reasoning": "...", "abstract_excerpt": "..."}]}

--- CLAIM/ABSTRACT PAIRS ---
{pairs_list}

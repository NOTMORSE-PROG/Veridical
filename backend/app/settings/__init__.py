"""Settings screen (F8.9 display slice, V-042, screen 4u): read-only
transparency for scoring/escalation thresholds and which prompt/model
versions actually ran, sourced live from the audit log. Password change
lives in `app.auth` (it's a credential operation, not a display concern).
"""
